	function MinMax(x, y) {
			if (x >= y) {alert("Минимальное значение = " + y);
			} else {alert("Минимальное значение = " + x);}
		}
		
		MinMax(-1, 44);
		
		function MinMax2(x, y) {
			if (x >= y) {alert ("Максимальное значение = " + x);
			} else {alert("Максимальное значение = " + y);}
		}
		MinMax2(-1, 44);