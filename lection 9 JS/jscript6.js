		var n = 8542;
		var k = 0;
		var s = "";
		while (n > 0) {
			k = n%10; // первая цифра справа - остаток от деления 
			//alert(k);
			s=s+k;
			n = (n-k)/10; // отбрасываем цифру справа 
		} 
		alert(s);